import { PieChartSharp } from "@mui/icons-material";
import React, { useState } from "react";
import BoxUi from "../../../../components/UiKit/BoxUi";
import AttentionCard from "../../components/AttentionCard/AttentionCard";
import OverviewHeader from "../../components/OverviewHeader/OverviewHeader";
import useStyles from "./Financial.style";
import { ReactComponent as SaveMoney } from "../../../../assets/svg/SaveMoney.svg"

export default function Financial({ children, ...props }) {
  const classes = useStyles();
  const [visibility, setVisibility] = useState(true);
  const ChartValues = {
    "Remaining Shares": 40,
    "Your Shares": 60,
    Sold: 0
  }
  return (
    <div className={`flex flex-col gap-[10px]`}>
      <OverviewHeader lockIcon={<PieChartSharp className="text-[16px]" />} ChartValues={ChartValues} visibility={visibility} setVisibility={setVisibility} />
      <div className={`flex flex-col lg:flex-row gap-[10px] leading-none`}>
        <AttentionCard className={`flex-[3]`} items={attentionItems} />
        <BoxUi className={`flex flex-[2] border border-solid rounded-[5px] lg:min-w-[389px] gap-[15px] items-center ${classes.share}`}>
          <div className={`flex flex-col gap-[10px] `} >
            <div className={`text-success text-[14px] font-bold`} >Share of Profit per Month</div>
            <div className={`opacity-50 text-[12px] leading-tight`} >By investing in 3CO Exchange you will receive a share of profit that generated by the exchange sucha as transaction fees. This profits of exchange will distribute to investors at the <span className={`font-bold`} >end of month.</span></div>
          </div>
          <div className={`flex flex-col w-[107px]`}><SaveMoney className={`w-[107px]`} /></div>
        </BoxUi>
      </div>
    </div>);
}

const attentionItems = [
  { title: "Minimum Stack", value: "1000", unit: "USDT", warning: true },
  { title: "Maximum Stack", value: "125000", unit: "USDT", warning: true },
  { title: "Price of 1 Share", value: "0.008", unit: "USDT", warning: true },
  { title: "Unlock Time", value: "365", unit: <span> Days <span className="hidden lg:inline-block" >(After Investment Complition)</span></span>, warning: true }
];
